# 🎰 Telegram-казино бот

Многофункциональный казино-бот для Telegram на **aiogram 3** + **SQLite**.

Игроки создают комнаты и играют 1 на 1 (🎲 🎯 ⚽ 🏀 🎳 🎰 🪙), а также
могут играть против **бота на бесплатных очках ⭐**. Есть встроенная
админ-панель: настройки экономики, заявки на пополнение/вывод, промокоды,
рекламные баннеры и донат по реквизитам карты.

---

## Возможности

- **7 игр** на нативных Telegram-эмодзи (честный случайный результат)
- **Игра 1 на 1**: комната живёт 15 сек, вступить можно кнопкой или ответом на сообщение
- **Игра с ботом** на отдельной бесплатной валюте ⭐ — не блокирует игры между людьми
- **Имитация броска**: реальная анимация кубика + пауза перед раскрытием результата
- **Авто-ход**: если игрок не ходит 30 сек, бот делает ход за него
- **Удаление сообщений игры** после результата (настраивается `RESULT_MSG_TTL`)
- **Красивый стиль сообщений** через HTML `blockquote` — плашка как у реплая, но без реплая
- **Две валюты**: 💰 баланс (игры между людьми) и ⭐ очки (игра с ботом)
- **Обмен валют** `/convert` по настраиваемым курсам
- **Пополнение счёта** через заявку админу
- **Вывод средств**, **переводы между игроками**
- **Промокоды** с копируемой ссылкой `https://t.me/c/CODE`
- **Донат по номеру карты** (реквизиты настраивает админ)
- **Админ-панель**: статистика, настройки, пополнение/списание, заявки, промокоды, баннеры
- **Авто-рассылка баннеров** по интервалу
- Команды в меню бота проставляются автоматически (админ видит `/admin`)

---

## Команды

### Для всех пользователей

| Команда | Описание |
|---|---|
| `/start` | Главное меню |
| `/games` | Создать комнату (игра 1 на 1) |
| `/bot` | Играть с ботом (очки ⭐) |
| `/balance` | Баланс 💰 и очки ⭐ |
| `/history` | История операций |
| `/stats` | Моя статистика |
| `/top` | Топ по балансу |
| `/topbot` | Топ по очкам бота |
| `/convert` | Обмен ⭐ ↔ 💰 |
| `/withdraw` | Заявка на вывод |
| `/deposit` | Заявка на пополнение |
| `/transfer` | Перевод баланса |
| `/donate` | Донат по карте |
| `/promo` | Активация промокода |
| `/help` | Помощь |

### Для админов

| Команда | Описание |
|---|---|
| `/admin` | Админ-панель (видна только админам) |

---

## Игры

| Ключ | Игра | Эмодзи | Результат |
|---|---|---|---|
| `dice` | Кубик | 🎲 | 1–6 |
| `dart` | Дротики | 🎯 | 0–5 (выпавшее − 1) |
| `football` | Футбол | ⚽ | 1–5, 3+ = гол |
| `basketball` | Баскетбол | 🏀 | 1–5, 3+ = гол |
| `bowling` | Боулинг | 🎳 | 1–6 |
| `slot` | Слот | 🎰 | 0–63 |
| `coin` | Монетка | 🪙 | Орёл/Решка (кнопками) |

**Как ходить:** отправьте эмодзи игры (можно ответом на сообщение игры)
или нажмите кнопку «🎲 Бросить». Побеждает тот, у кого значение больше.
При **ничьей** ставки возвращаются (в том числе в игре с ботом — бот не выигрывает).

---

## Установка и запуск

```bash
# 1. Установка зависимостей
pip install -r requirements.txt

# 2. Настройка .env (см. раздел «Конфигурация»)
cp .env.example .env
# отредактируйте BOT_TOKEN и ADMIN_IDS

# 3. Запуск
python main.py
```

> ⚠️ Важно: должен быть запущен **только один экземпляр** бота.
> Если запустить второй — Telegram вернёт `TelegramConflictError`.

Windows (консоль cp1251, чтобы кириллица не ломалась):
```powershell
$env:PYTHONIOENCODING="utf-8"; python main.py
```

---

## Конфигурация (`.env`)

| Переменная | По умолчанию | Описание |
|---|---|---|
| `BOT_TOKEN` | — | Токен бота от @BotFather |
| `ADMIN_IDS` | — | ID админов через запятую |
| `START_BALANCE` | `1000` | Стартовый баланс новичка 💰 |
| `START_POINTS` | `2000` | Стартовые очки новичка ⭐ |
| `MIN_BET` / `MAX_BET` | `1` / `1000000` | Лимиты ставок 💰 |
| `COMMISSION_PERCENT` | `0` | Комиссия казино, % |
| `POINTS_MIN_BET` / `POINTS_MAX_BET` | `10` / `100000` | Лимиты ставок ⭐ |
| `POINTS_BUY_RATE` | `150` | 100 💰 → N ⭐ (курс покупки) |
| `POINTS_SELL_RATE` | `60` | 100 ⭐ → N 💰 (курс продажи) |
| `WITHDRAW_MIN` | `1000` | Минимальная сумма вывода |
| `WITHDRAW_NOTE` | `Выплаты...` | Текст о способе выплаты |
| `DONATE_CARD_TYPE` | `Сбербанк` | Тип карты для доната |
| `DONATE_CARD_NUMBER` | пусто | Номер карты для доната |
| `DONATE_CARD_NAME` | пусто | ФИО получателя |
| `JOIN_TIMEOUT` | `15` | Секунды ожидания соперника |
| `MOVE_TIMEOUT` | `30` | Секунды ожидания хода |
| `DICE_ROLL_DELAY` | `3` | Пауза «вращения кубика», сек |
| `RESULT_MSG_TTL` | `15` | Через сколько сек удалять сообщение результата |
| `BANNER_CHECK_INTERVAL` | `30` | Интервал проверки авто-рассылок баннеров |
| `DB_PATH` | `casino.db` | Путь к файлу БД |

### Настройки из админ-панели

Почти всё из таблицы выше админ может менять прямо в боте:
`/admin → ⚙️ Настройки`. Значения применяются сразу (числовые и строковые,
например реквизиты донат-карты).

---

## Админ-панель

`/admin` → кнопки:

- **📊 Статистика** — пользователи, балансы, ставки, комнаты
- **⚙️ Настройки** — все параметры экономики и таймингов, реквизиты доната
- **💰 Пополнить баланс** — начислить/списать игроку (по ID или @username)
- **💳 Заявки на пополнение** — подтвердить/отклонить
- **💸 Заявки на вывод** — подтвердить/отклонить (возврат средств при отказе)
- **🎁 Создать промокод** — код + сумма + лимит активаций
- **📜 Список промокодов**
- **🖼 Баннеры** — создание, ручная рассылка, авто-рассылка по интервалу
- **🧾 Все операции** — последние транзакции

---

## Структура проекта

```
CasinoGame/
├── main.py               # точка входа: роутеры, команды меню, планировщик баннеров
├── config.py             # .env + настройки, редактируемые админом (SETTINGS)
├── database.py           # SQLite: пользователи, транзакции, промокоды, заявки, баннеры
├── keyboards.py          # клавиатуры и тексты кнопок
├── utils.py              # экранирование HTML, форматирование чисел/времени
├── run_tests.py          # смоук-тест всех фич (104 теста)
├── requirements.txt
├── .env.example          # шаблон конфигурации
└── services/
    ├── rooms.py          # менеджер комнат, игровой цикл, игра с ботом
    ├── games.py          # справочник игр
    └── banners.py        # рассылка баннеров
└── handlers/
    ├── start.py          # /start, баланс, история, статистика, топы, помощь
    ├── rooms.py          # создание комнат, вступление, ходы, сдача
    ├── convert.py        # обмен ⭐ ↔ 💰
    ├── deposit.py        # заявки на пополнение
    ├── withdraw.py       # заявки на вывод
    ├── transfer.py       # переводы и донат
    ├── promo.py          # промокоды
    ├── banner.py         # админка баннеров
    ├── admin.py          # админ-панель
    └── common.py         # глобальная отмена (cancel)
```

---

## Архитектура игрового цикла

- `RoomManager` держит комнаты и активность игроков. Два менеджера:
  `room_manager` (💰 игры между людьми) и `bot_room_manager` (⭐ игра с ботом).
- Комната знает свой менеджер (`room.manager`), все ходы и таймауты идут через него —
  это исключает конфликт «уже в активной игре» между режимами.
- Все сообщения бота во время игры собираются в `room.bot_msgs` и удаляются
  одним таском через `RESULT_MSG_TTL` секунд после завершения.
- Кубик игрока (нативное сообщение) тоже помечается на удаление
  (`room.extra_msgs`) — в группах, где бот админ, он убирается и это.
- Имитация броска: `send_dice` (анимация) → пауза `DICE_ROLL_DELAY` → reveal результата.

---

## Тесты

Запуск (Windows):

```powershell
$env:PYTHONIOENCODING="utf-8"; python run_tests.py
```

Запуск (Linux/macOS):

```bash
PYTHONIOENCODING=utf-8 python run_tests.py
```

Покрытие: экономика (баланс/очки/топы), промокоды, переводы, заявки на
вывод/пополнение, настройки (числовые и строковые), статистика, баннеры,
игровой цикл (ничьи, авто-ход, сдача, отмена, удаление сообщений),
игра с ботом (включая ничью — возврат ставки), донат (формат номера карты).

---

## База данных (SQLite)

| Таблица | Назначение |
|---|---|
| `users` | игроки: баланс 💰, очки ⭐ |
| `transactions` | все операции (пополнения, ставки, выигрыши, переводы…) |
| `promocodes` / `promo_uses` | промокоды и их активации |
| `withdrawals` | заявки на вывод |
| `deposits` | заявки на пополнение |
| `banners` / `banner_sends` | баннеры и доставки |
| `game_stats` | статистика игр |
| `settings` | настройки, изменённые через админ-панель |
